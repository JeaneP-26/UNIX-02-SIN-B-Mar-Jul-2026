pwd #Print Working Directory, Show the path of the current directory
cd / #Go to the system’s root directory
ls -F #List files and folders, adding a symbol at the end to show their type
ls -i #Show the inodes
echo "hola">test.txt #Creates a file and writes “hola” inside it
stat test.txt #Shows detailed information about the test.txt file, such as size, permissions, dates, and inode number.